# Function: useChunk()

> **useChunk**&lt;`T`&gt;(`array`, `size`): `T`[][]

Defined in: [src/core/services/utils.service.ts:9](https://github.com/senseikatana/katanakit-js/blob/3163abef08c959a49d8e5fdfc946044012dab465/src/core/services/utils.service.ts#L9)

Pure: split an array into chunks of `size` (throws if size ≤ 0).

## Type Parameters

### T

`T`

## Parameters

### array

`T`[]

### size

`number`

## Returns

`T`[][]
