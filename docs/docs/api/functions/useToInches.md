# Function: useToInches()

> **useToInches**(`cm`, `locale?`, `digits?`): `string`

Defined in: [src/core/services/formatter.service.ts:87](https://github.com/senseikatana/katanakit-js/blob/7327444cca55df05754ea3a25325f2d544b9811c/src/core/services/formatter.service.ts#L87)

Pure: centimeters → inches (formatted).

## Parameters

### cm

`number`

### locale?

[`Locale`](../type-aliases/Locale.md) = `"en"`

### digits?

`number` = `2`

## Returns

`string`
